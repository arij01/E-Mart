version: "3.3"
services:
  mongodb:
    image: mongo
    container_name: mongo_db
    ports:
      - 27017:27017
    volumes:
      - mongo:/data
    environment:
      - MONGO_INITDB_ROOT_USERNAME=arij
      - MONGO_INITDB_ROOT_PASSWORD=arij

volumes:
  mongo: